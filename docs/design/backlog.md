# 작업 백로그

> 합의됐으나 아직 착수 전인 작업 모음. 우선순위는 사용자와 조율. 완료 시 체크 + 커밋 해시.

## UI / 시각화 (텍스트 → 그래픽)

사용자 방향(2026-06-08): 게임이 전체적으로 텍스트 기반이라, **그래픽을 통한 직관적 시각 정보**를 늘린다.

- [ ] **스킬 선택 시각화** — 전체 스킬 트리를 아이콘 목록/마인드맵 형식으로 표시. 마우스 오버·포커스 시 해당 스킬 설명을 띄움. 목적: 플레이어가 *미래 가치*(같은 라인 T2·T3에 뭐가 오는지)까지 보고 투자 결정. 현재 `LevelUpOverlay`는 픽 3장 카드만 보여줘 라인 점증이 안 보임. 트리 노드 뷰로 확장.
- [ ] **맵 진행 시각화** — `RouteMap` 선택 화면 옆에 지나온 경로(`GameState.route_history`)와 앞으로 남은 단계 수(`current_stage`/`effective_total_stages`)를 노드맵/진행바로. 데이터는 이미 존재, 그리기만 하면 됨.
- [ ] **오프닝 인트로 그림** — `Briefing` 인트로(SILO-7/PALIMPSEST)에 간략한 그림/비주얼을 곁들여 몰입 강화. (이전에 "인트로 몰입" 후속으로 메모한 항목)
- 공통: 아이콘/그림 자산 필요 여부 검토. 우선 도형·이모지·색으로 시작 가능(자산 없이 프로토타입).

## 기능

- [ ] **해상도 조절** — 설정(`Settings.gd`)에 해상도/창 모드 옵션 추가. (2026-06-08 사용자 요청, "다음 작업으로")
- [ ] **글라이드 유리한 맵** — 저격병이 플랫폼 한가운데 멀뚱히 서 있으면 글라이드가 영영 안 쓰임(사용자 통찰). 저격병을 글라이드로만 닿거나 회피하는 위치(고지대 단독 발판 / 긴 갭 너머 / 긴 낙하 구간)에 두는 맵이 필요. 신규 맵 vs 기존 저격 맵(rooftops/watchtower) 개조 — 방향 미정.

## 정리

- [ ] **outdated docs** — `STORY_REDESIGN_v1~v3` + `v1/v2_REVIEW`(5개)는 과거 스토리 재설계 협업 라운드. 현재 캐논은 `STORY.md`·`STORY_HANDOFF.md`에 통합됨. 처리 방향(아카이브 이동 / superseded 헤더 / 삭제) 미정.
- [ ] **STORY.md §10 스테일분** — Stage 5/6 브리핑·ward 복선 등 이미 구현됐으나 문서에 미반영된 항목(STORY 갱신 에이전트가 범위 밖이라 보류).
- [ ] **barrier 라인 desc/효과 대조** — growth_system 갱신 시 후속으로 남긴 확인 항목.
