# 작업 백로그

> 합의됐으나 아직 착수 전인 작업 모음. 우선순위는 사용자와 조율. 완료 시 체크 + 커밋 해시.

## ★ 추천 작업 (2026-06-08 정리 — 돌아왔을 때 고르기 좋게)

> 사용자 요청으로 "다음에 할 만한 것"을 한눈에 모음. 아래 상세 섹션의 발췌 + 신규 제안.

**A. 직접 확인이 필요한 것 (내가 못 함 — 환경/플레이)**
- **내보낸 빌드로 체감 확인** — 해상도 실제 적용·텍스트 선명도·비네트 농도(0.10)·맵 간 어두움 지속.
  (에디터 임베드/HiDPI라 에디터 미리보기론 흐릿. export 설정·실행 도와줄 수 있음.)
- **VEIL 추천 임계값 튜닝** — `PERF_SKILLED_HITS_MAX=1`/`STRUGGLE_MIN=4`가 체감과 맞는지 플레이로.

**B. 콘텐츠/게임플레이 (방향 결정 필요)**
- **글라이드 유리한 맵 + 저격수 배치/발사 함정** — 신규 맵 vs 기존 개조(방향 미정). 내가 구체안 제안 가능.
- **shield T3 "방어막 재충전" 처리** — 현재 미구현인데 트리엔 표시됨(desc≠효과). 구현하거나 트리에서 빼기.

**C. UI/시각 — "텍스트→그래픽" 계속** (2026-06-08 자율 진행 — 검토만)
- [x] **전역 텍스트 아웃라인 통일** (c306d22) — `assets/theme/default_theme.tres` 기본 테마로 전체 적용.
- [x] **비네트 셰이더화** (7ffb9a4) — `veil_vignette.gdshader` + 디더로 밴딩 완전 제거.
- [x] **아이콘 확산** (795f5e6) — `EnemyIcon.gd` 5종, 도감 조우 카드에 적 아이콘.
- [x] **오프닝/브리핑 비주얼 고도화** (d580bd3) — VEIL 눈을 전 스테이지 브리핑에 표시(목표물 아이콘은 stage 0만). ACT3 시야 붕괴 시 눈이 흐려짐(서사 연결). → 추후 실제 자산 도입은 별도.
- [x] **RouteMap 맵 카드 적 아이콘** (30855a7) — 각 카드 하단에 등장 적 타입(`EnemyIcon`). 본 적은 아이콘, 미확인은 "?". 맵 선택 시 위협 직관화 + 스킬-적 상성 선택 보조.

**D. 코드 정리/리팩토링** (2026-06-08 자율 진행 — 검토만)
- [x] **FAMILY_COLORS 단일화** (4abf394) — `SkillTreeData.FAMILY_COLORS` 단일 소스로.
- [ ] **Stage.gd 분리** — **평가 결과 자율 보류**: 3580줄 god-object, 수십 개 공유 멤버 상태로 엮임.
  분리하면 상태 전달이 필요해 회귀 위험 크고, 게임플레이(보스·웨이브·챌린지)는 스크린샷으로 검증 불가
  → 플레이테스트 동반 필수. **단계적 안전 계획**: ① 가장 독립적인 `_ambience_*`(12개, 순수 장식 빌드)를
  `StageAmbience` 헬퍼로 먼저 추출(host 노드 전달) → ② HUD 빌드/갱신 묶음 → ③ 적 스폰/웨이브.
  각 단계 후 전 스테이지 타입 플레이 확인. 사용자가 시간 잡고 함께 검증할 때 착수 권장.
- [ ] **Pretendard-Medium 도입** — 외부 폰트 자산 필요. 전역 아웃라인 테마로 "중간 굵기" 체감은 확보돼 우선순위 낮음.

**E. 문서/정리**
- STORY.md §2(맵 표) 등 남은 스테일분 점검. INDEX "최근 시스템" 최신화(이번 UI 작업 반영 — 일부 진행).


## UI / 시각화 (텍스트 → 그래픽)

사용자 방향(2026-06-08): 게임이 전체적으로 텍스트 기반이라, **그래픽을 통한 직관적 시각 정보**를 늘린다.

- [x] **스킬 선택 시각화** (6b574fa) — 독립 트리 오버레이(`SkillTreeOverlay`). 8라인×3티어 + 베이스라인을 3계열 컬럼으로, 글리프로 상태(● 보유 / ◆ 다음 / ○ 잠김). 포커스·호버 시 하단 패널에 T1~T3 전체 펼침 → 미래 가치 가시화. 일시정지 메뉴·레벨업 화면 버튼으로 진입.
- [x] **맵 진행 시각화** (17b206f) — `RouteMap` 헤더와 카드 사이에 가로 진행 노드맵. 지나온 단계(● + 선택한 맵 이름) / 현재(◆ "지금", VEIL 톤색) / 남은 단계(○ "?"). `RouteData.name_for_id` 헬퍼 추가.
- [x] **오프닝 인트로 그림** (42a2b9c) — `BriefingVisual.gd`: 자산 없이 `_draw` 도형으로 그린 VEIL 감시 조리개/눈(바깥 링·눈금·십자선·회전 스캔 스윕·맥동 눈동자, 등장 시 조리개 열림). stage 0(OPERATION PALIMPSEST) 진입에서만 패널 위에 표시. "연결됐어요… 제가 봐줄게요" 정체성 시각화. → 추후 자산/추가 비주얼로 고도화 가능.
- 공통: 아이콘/그림 자산 필요 여부 검토. 우선 도형·이모지·색으로 시작 가능(자산 없이 프로토타입).

## 기능

- [x] **해상도 조절** (a00421c) — `Settings.gd` "화면" 탭에 전체화면 토글 + 창 크기 프리셋(1280x720/1600x900/1920x1080). `GameState.apply_display_settings()`로 즉시 반영·`settings.cfg` 영속, 부팅 시 Main.gd가 적용. 웹은 창 크기를 브라우저가 정해 전체화면 토글만 노출.
- [ ] **글라이드 유리한 맵** — 저격병이 플랫폼 한가운데 멀뚱히 서 있으면 글라이드가 영영 안 쓰임(사용자 통찰). 저격병을 글라이드로만 닿거나 회피하는 위치(고지대 단독 발판 / 긴 갭 너머 / 긴 낙하 구간)에 두는 맵이 필요. 신규 맵 vs 기존 저격 맵(rooftops/watchtower) 개조 — 방향 미정.
  - **2026-06-08 감사 결과**(`map_audit.md`): 현재 저격수는 모두 고지대 발판(평지 방치 없음), 글라이드/대시 유리 지형도 5개 맵(subway/cooling/blackout/rooftops/watchtower)에 이미 존재. 즉 빈칸은 "유리 지형 부재"가 아니라 ① **회피 강제 저격수**(못 잡고 지나가야 하는 위치) ② **유인 강화**(글라이드/대시로만 닿는 지름길·보상)임.
- [~] **저격수 배치 / 발사 함정** (2026-06-08 사용자 요청) — 발사 함정 프로토타입 착수(acecc86), 저격수 재배치 미착수.
  - [x] **발사 함정(총알 트랩)** (acecc86→09d6f08→6a0b318) — `BulletTrap.gd`(periodic/triggered) + `LaserTripwire.gd`(탐지선과 포탑 분리). 표면 장착·⚠·VEIL "파괴 불가" 안내. 총알 velocity 방향 회전, 속도 460. 확산: subway(탐지선+포탑 분리)·cooling(파이프 가로 포탑)·ward(하향 포탑). → **남음**: 더 많은 맵·버스트 변형 튜닝.
  - [x] **저격수 재배치** (eee9cfb) — rooftops·watchtower·cooling의 sniper를 메인 경로 밖 **측면 단독 둥지**(엇갈린 좌/우, 경로를 사선으로 내려다봄)로. watchtower의 patrol과 같은 좌표 문제 해소. subway(지붕)·datacenter(웨이브 분리)는 유지. → **남음**: LoS/난이도 플레이테스트 튜닝(엄폐 충분한지, 사선 노출 시간).
  - [x] **못 잡는 적 안내** (사용자 요청) — `MapData` `nest_snipers` 플래그(rooftops·watchtower·cooling)로 측면 둥지 저격수를 `_spawn_enemy`에서 `avoid_only` 메타 태깅. `Stage._tick_avoid_warning`이 근접 시 1회 "정면으론 못 잡으니 사선 피하거나 글라이드로 덮쳐라" 안내(글라이드-저격 상성 학습). ACT3 시야 붕괴(`veil_degraded`) 후속 맵 진입 시 `_arm_degraded_hazard_warning`이 함정/매복을 마커로 못 짚어준다고 미리 경고 + 트랩 근접 경고도 붕괴 톤 변형. 기존 `_show_veil_subtitle` 자막 채널 재사용. → **남음**: 둥지 사선 노출·안내 타이밍 플레이테스트.

- [ ] **스킬 재설계 제안** (2026-06-08 플레이테스트) — 결정 후 구현:
  - [x] **글라이드 T1 재설계** (0837140) — 패시브 슬로우낙하(홀드 불필요) + 공중 점프 1회 추가(최대 3단). T2/T3 공중사격 판정도 홀드 제거. 검증: 삼단 점프·홀드 없는 낙하 캡. → **후속(미착수)**: 글라이드/대시로만 닿는 숨은 보상을 맵에 배치(이제 도달 가능해짐).
  - **공격 속도 라인 부재** — 전용 공속 패시브 없음(fire_boost T2의 짧은 가속뿐). 추가 검토 vs 아래 차지 스킬로 대체.
  - **유도 차지 사격 스킬** (신규 아이디어) — 공격 꾹 누르면 연사 대신 **누른 시간만큼 유도되는 강탄** 발사. 매력적이나 차지 시스템 신규 + multishot T3/glide T3 유도와 역할 중복 검토 필요.
- [ ] **VEIL 추천 임계값 튜닝** (재설계 0c1613a 완료) — 실력 판정 임계값(`PERF_SKILLED_HITS_MAX=1`, `PERF_STRUGGLE_HITS_MIN=4`, 최근 2스테이지 평균)이 체감과 맞는지 플레이테스트로 보정. 너무 자주/드물게 "고전·능숙"으로 갈리면 조정. 시간·처치 수는 추적은 하되(`last_stage_secs`) 판정엔 미사용 — 맵 난이도 편차 노이즈 때문. 필요 시 편입 검토.

## 정리

- [x] **outdated docs** (정리) — `STORY_REDESIGN_v1~v3` + `v1/v2_REVIEW`(5개)를 `docs/archive/`로 이동(하드 삭제 대신, 설계 흐름 이력 보존). `docs/archive/README.md`에 캐논(`STORY.md`/`STORY_HANDOFF.md`) 안내. `INDEX.md`에 아카이브 규칙 한 줄 추가. 활성 문서·코드 참조 없음 확인 후 이동.
- [x] **STORY.md 인벤토리 스테일분** (정리) — Part II §1(첫 브리핑)·§3(Stage 브리핑 풀)을 라이브 `VeilDialogue` 풀에 재동기화. Stage 5/6 채움(미작성→실제 텍스트), Stage 3에 ward/봉인 복선 반영, v2/v3 시야 역전 재설계로 드리프트된 §1·Stage 0~4·인트로까지 일치. §3 헤더에 "코드 풀의 스냅샷" 명시 + 스토리 모드 별도 풀 안내. (§2 맵 표는 의도된 발췌라 유지.)
- [x] **barrier 라인 desc/효과 대조** (정리) — T1/T2/T3 desc와 `Player.gd` 구현 일치 확인, 불일치 없음. growth_system.md에 결과 기록. (별개로 shield T3 "방어막 재충전"은 여전히 미구현 — growth_system §8에서 추적 중.)
